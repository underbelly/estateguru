{
  "name": "Estate Guru",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass scss/style.scss css/style.css --watch",
    "build": "esbuild js/app.js --bundle --outfile=dist/app.bundle.js --format=iife --global-name=AppModule --minify --platform=browser",
    "build-esm": "esbuild js/app.js --bundle --outfile=dist/app.bundle.js --format=esm --minify --platform=browser",
    "dev": "esbuild js/app.js --bundle --outfile=dist/app.bundle.js --format=iife --global-name=AppModule --watch --platform=browser",
    "dev:all": "concurrently \"npm run sass\" \"npm run dev\"",
    "start": "npm run dev:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.19.0",
    "sass": "^1.89.0"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.3.9",
    "smooothy": "^0.0.26"
  }
}
